{
  "private": true,
  "name": "changelog-writer",
  "version": "1.0.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Kleshzz/changelog-writer.git"
  },
  "bugs": {
    "url": "https://github.com/Kleshzz/changelog-writer/issues"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --minify",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run build && npx -y ts-node tests/action.test.ts"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/exec": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^25",
    "@typescript-eslint/eslint-plugin": "^8",
    "@typescript-eslint/parser": "^8",
    "eslint": "^10",
    "esbuild": "^0.28.0",
    "prettier": "^3",
    "typescript": "^6"
  }
}
